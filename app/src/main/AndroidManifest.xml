<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XimcaClinicApp">

        <!-- Pantalla de inicio: Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registro de nuevo médico -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Dashboard principal (post-login) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Lista de pacientes (RecyclerView CRUD) -->
        <activity
            android:name=".PacienteListActivity"
            android:exported="false" />

        <!-- Detalle de un paciente -->
        <activity
            android:name=".PacienteDetailActivity"
            android:exported="false" />

        <!-- Formulario crear / editar paciente -->
        <activity
            android:name=".PacienteFormActivity"
            android:exported="false" />

    </application>

</manifest>
